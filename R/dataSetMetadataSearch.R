#'@title Dataset Metadata Search
#'@export
dataSetMetadataSearch <- function(keyword = NULL, filter = character(0), limit = 100L, offset = 0L) {
  # construct url
  
  # use the api in DataTaipei.R to return the result
}